// lib/firebase_options.dart
// Stub used for Mock Mode / web builds before running `flutterfire configure`.

import 'package:firebase_core/firebase_core.dart' show FirebaseOptions;

class DefaultFirebaseOptions {
  // For Mock Mode we don't need real options; `null` is fine for compile-time.
  static const FirebaseOptions? currentPlatform = null;

  const DefaultFirebaseOptions._();
}
